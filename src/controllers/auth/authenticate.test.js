describe("Test controller", () => {
  it("should return same data", async () => {
    // const res = mockResponse(data, 200);
    // const req = mockRequest({}, {});
    // let d = await getCategories(req, res);
    //expect(res.json).toHaveBeenCalledWith(data);
  });
});
